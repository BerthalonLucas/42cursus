

if (argv[1] = 1) /* si l'arg 1 est = 1 on doit faire un affichage en colonne ou au moins un A */
{
	print ("A"); /* donc on affiche deja un A mais on connait pas encore quel est l'argument 2 */
	if (argv(2) = 1) /* si on est dans ce cas la l ennonce dit de n afficher que le A initial tout seul donc on sort */
		exit (0);
	{
		if(argv(2) = 2)  /* pour le moment on traite le cas limite du deuxieme argument = 2 avec un code fixe car on doit afficher un autre A en colonne */
		{
			print ("\n")
			print ("A)
			exit (0) /* et on sort */
		}
		/* la on rentre dans un cas ou l'arg 1 = 1 et l'arg 2 est supérieur a 2 donc il faut afficher en colonne des caracteres B */
	}

}
/* sinon cad si argv[1] est superieur a 1 va falloir faire autre chose */
{ /* la on est dans le cas sinon de si premier argument = 1 */

}
